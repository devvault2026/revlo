import React from 'react';
import { motion } from 'framer-motion';

const Results: React.FC = () => {
    const results = [
        {
            number: '10x',
            label: 'Operational Leverage',
            description: 'The standard multiplier in output achieved by replacing manual workflows with autonomous neural cores.',
            color: 'text-red-500',
        },
        {
            number: '0%',
            label: 'Human Overhead',
            description: 'Our systems require zero manual intervention post-deployment. We build machines that scale without hiring.',
            color: 'text-white',
        },
        {
            number: '$1.2M+',
            label: 'Autonomous Revenue',
            description: 'Verifiable revenue generated by our self-selling entities for partner ecosystems in the last quarter.',
            color: 'text-red-600',
        },
        {
            number: '99.9%',
            label: 'Signal Resonance',
            description: 'Our Market Scout engines detect and engage leads with human-grade precision at machine-grade volume.',
            color: 'text-white',
        },
    ];

    return (
        <section id="results" className="py-40 bg-black relative">
            <div className="max-w-[1800px] mx-auto px-6 lg:px-12 relative z-10">
                {/* Section Header */}
                <motion.div
                    initial={{ opacity: 0, y: 30 }}
                    whileInView={{ opacity: 1, y: 0 }}
                    viewport={{ once: true }}
                    className="mb-24 flex flex-col md:flex-row items-end justify-between gap-12"
                >
                    <div className="max-w-3xl space-y-6">
                        <div className="inline-block px-5 py-1.5 border border-red-600/30 bg-red-600/5 text-red-500 text-[10px] font-black uppercase tracking-[0.4em]">
                            Verifiable Dominance
                        </div>
                        <h2 className="text-[clamp(2.5rem,6vw,6.5rem)] font-black tracking-tighter text-white uppercase leading-[0.9]">
                            VERIFIABLE <br /><span className="gradient-text-alt">OUTCOMES.</span>
                        </h2>
                    </div>

                    <div className="max-w-lg text-right">
                        <p className="text-xl lg:text-2xl text-slate-500 leading-tight font-black uppercase">
                            "Data is binary. Our superiority is <span className="text-white">absolute.</span>"
                        </p>
                    </div>
                </motion.div>

                {/* Results Grid */}
                <div className="grid md:grid-cols-2 lg:grid-cols-4 gap-px bg-white/[0.05] border border-white/[0.05] overflow-hidden">
                    {results.map((result, index) => (
                        <motion.div
                            key={index}
                            initial={{ opacity: 0 }}
                            whileInView={{ opacity: 1 }}
                            transition={{ duration: 0.8, delay: index * 0.1 }}
                            viewport={{ once: true }}
                            className="group bg-black p-12 hover:bg-zinc-950/50 transition-colors duration-500"
                        >
                            <motion.div
                                initial={{ scale: 0.9, opacity: 0 }}
                                whileInView={{ scale: 1, opacity: 1 }}
                                transition={{ duration: 0.8, delay: index * 0.1 + 0.2 }}
                                viewport={{ once: true }}
                                className={`text-5xl lg:text-7xl font-black mb-10 tracking-tighter ${result.color}`}
                            >
                                {result.number}
                            </motion.div>

                            <h3 className="text-lg font-black mb-6 uppercase tracking-widest text-white">
                                {result.label}
                            </h3>

                            <p className="text-slate-500 text-base font-medium leading-snug group-hover:text-slate-300 transition-colors">
                                {result.description}
                            </p>
                        </motion.div>
                    ))}
                </div>
            </div>

            {/* Design accents */}
            <div className="absolute top-0 left-0 w-full h-[1px] bg-gradient-to-r from-transparent via-white/5 to-transparent" />
        </section>
    );
};

export default Results;
